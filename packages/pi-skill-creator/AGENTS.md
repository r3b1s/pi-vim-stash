# ⚠️ Wrong directory

You are inside a **package subdirectory**. Pi should be started from the repo root.

```bash
cd /home/t/Local/personal/pi-things
```

The root `AGENTS.md` and `.pi/settings.json` live there.
